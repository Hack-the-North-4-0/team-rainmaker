service: api

provider:
  name: aws
  runtime: nodejs12.x
  region: eu-west-1

functions:
  createGame:
    handler: handler.createGame
    events:
      - http:
          path: /games
          method: post
          cors: true
